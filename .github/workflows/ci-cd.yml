name: CI/CD Pipeline

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kode
        uses: actions/checkout@v2

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/myapp:latest .

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Push Docker Image ke Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/myapp:latest

  test-ssh:
    runs-on: self-hosted
    steps:
      - name: Cek SSH ke Server
        uses: fifsky/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          pass: ${{ secrets.SSH_PASSWORD }} # Pastikan ini ada di GitHub Secrets
          command: |
            echo "✅ Berhasil masuk ke server!"
            whoami
            echo "✅ Cek home directory:"
            ls -lah ~
            echo "✅ Cek OS dan kernel:"
            uname -a
            echo "✅ Cek konektivitas internet:"
            ping -c 4 google.com || echo "🚨 Tidak bisa akses internet!"
            echo "✅ Cek status SSH service:"
            systemctl status ssh || echo "🚨 SSH service bermasalah!"

  deploy:
    needs: [build, test-ssh]
    runs-on: self-hosted
    steps:
      - name: Deploy ke Server
        uses: fifsky/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          pass: ${{ secrets.SSH_PASSWORD }}
          command: |
            echo "✅ Login ke server berhasil"

            echo "✅ Pastikan Docker berjalan"
            sudo systemctl start docker
            sudo systemctl enable docker

            echo "✅ Login ke Docker Hub"
            echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

            echo "✅ Pull image terbaru"
            docker pull ${{ secrets.DOCKER_USERNAME }}/myapp:latest

            echo "✅ Hentikan container lama"
            docker stop myapp || echo "⚠️ Container tidak ditemukan, lanjut ke langkah berikutnya."

            echo "✅ Hapus container lama"
            docker rm myapp || echo "⚠️ Container tidak ditemukan, lanjut ke langkah berikutnya."

            echo "✅ Jalankan container baru"
            docker run -d -p 8000:80 --name myapp ${{ secrets.DOCKER_USERNAME }}/myapp:latest
